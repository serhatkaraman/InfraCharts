apiVersion: v1
kind: ConfigMap
metadata:
  name: jenkins-pipeline
  namespace: {{ .Release.Namespace }}
data:
  Jenkinsfile: |
{{ .Values.pipeline.jenkinsfile | indent 4 }}
  DeployJenkinsfile: |
{{ .Values.pipeline.deployJenkinsfile | indent 4 }}
